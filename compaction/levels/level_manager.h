#pragma once


/**
 * Each level consists of multiple SST files. We need to be able to find clear cut boundaries between key ranges for SST Compaction,
 * and maintain counts for the number of references are there to that specific SST file
 */
class LevelManager {
    
};

